import { RepoSpec } from "@moonwall/types";

const repo: RepoSpec = {
  name: "polkadot",
  binaries: [{ name: "polkadot" }],
  ghAuthor: "paritytech",
  ghRepo: "polkadot-sdk",
};

export default repo;
