{
  "name": "@moonwall/monorepo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@7.23.0",
  "description": "Testing framework for the Moon family of projects",
  "scripts": {
    "build": "pnpm -r --filter='./packages/**' run build",
    "lint": "prettier --write --ignore-path .gitignore '**/*.(yml|js|ts|json)'",
    "release": "",
    "test": "moonwall test 'dev_multi basic chopsticks' ",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@acala-network/chopsticks": "^0.5.1",
    "moonwall": "workspace:*",
    "pnpm": "7.23.0",
    "rimraf": "^4.1.2",
    "@types/node": "^18.14.6",
    "typescript": "^4.8.4"
  }
}
